stages:
  - build

variables:
  GRADLE_USER_HOME: "$CI_PROJECT_DIR/.gradle"

build:
  stage: build
  image: gradle:9.4.1-jdk-26
  script:
    - gradle clean build
  artifacts:
      paths:
        - build/libs/
      expire_in: 1 week